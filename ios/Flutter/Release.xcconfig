#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "Generated.xcconfig"
# Signature gérée par Codemagic (xcode-project use-profiles → manuelle).
# Ne PAS forcer DEVELOPMENT_TEAM / CODE_SIGN_STYLE=Automatic ici : ça pousse
# Flutter vers la signature automatique (nécessite un compte Xcode connecté,
# absent en CI) et casse le profil manuel App Store.
