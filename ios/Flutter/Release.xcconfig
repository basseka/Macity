#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "Generated.xcconfig"
// Signature geree par Codemagic (xcode-project use-profiles -> manuelle).
// Ne PAS forcer DEVELOPMENT_TEAM / CODE_SIGN_STYLE=Automatic ici : ca pousse
// Flutter vers la signature automatique (necessite un compte Xcode connecte,
// absent en CI) et casse le profil manuel App Store.
